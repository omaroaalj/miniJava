import java.util.Scanner;
import java.lang.reflect.*;

double FACTOR = 5.0 / 9;
double OFFSET = 32;

double FtoC(double F) {
    double C = FACTOR - 1; //this.FACTOR * (F - OFFSET);
    return C;
}

main() {
    FtoC(5.0);
}